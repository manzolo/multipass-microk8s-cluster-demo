upstream k8s-cluster-go {
    server ${VM_MAIN_NAME}.loc:31001;
    server ${VM_NODE_PREFIX}1.loc:31001;
    server ${VM_NODE_PREFIX}2.loc:31001;
}

server {
    listen 80;
    server_name demo-go.loc;

    location / {
        proxy_pass http://k8s-cluster-go;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

upstream k8s-cluster-php {
    server ${VM_MAIN_NAME}.loc:31002;
    server ${VM_NODE_PREFIX}1.loc:31002;
    server ${VM_NODE_PREFIX}2.loc:31002;
}

server {
    listen 80;
    server_name demo-php.loc;

    location / {
        proxy_pass http://k8s-cluster-php;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}